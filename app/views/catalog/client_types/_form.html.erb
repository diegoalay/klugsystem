<%= form_with(model: catalog_client_type) do |form| %>
  <% if catalog_client_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catalog_client_type.errors.count, "error") %> prohibited this catalog_client_type from being saved:</h2>

      <ul>
        <% catalog_client_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
