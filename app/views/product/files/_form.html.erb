<%= form_with(model: product_file) do |form| %>
  <% if product_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_file.errors.count, "error") %> prohibited this product_file from being saved:</h2>

      <ul>
        <% product_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
