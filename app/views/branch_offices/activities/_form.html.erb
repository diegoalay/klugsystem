<%= form_with(model: branch_offices_activity) do |form| %>
  <% if branch_offices_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(branch_offices_activity.errors.count, "error") %> prohibited this branch_offices_activity from being saved:</h2>

      <ul>
        <% branch_offices_activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
